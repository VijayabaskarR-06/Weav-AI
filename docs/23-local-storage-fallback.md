# Local Storage Fallback

The browser demo keeps a local fallback so the UI remains usable if the backend is temporarily offline.

When backend is available, signup, measurements, feedback, and price prediction use FastAPI.
