# Known Limitations

The price predictor is a backend prediction model, not an official store price feed.

Real live prices would require store APIs, affiliate APIs, or a compliant catalog ingestion service.
